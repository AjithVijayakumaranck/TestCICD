name: Dealnbuy deployment test

on:
  push:
    branches:
      - master  # Change to your deployment branch if different

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    # - name: Set up Node.js
    #   uses: actions/setup-node@v2
    #   with:
    #     node-version: '18'  # Specify your Node.js version
    # - name: Install backend dependencies
    #   run: |
    #     cd ./Backend
    #     npm install

    # - name: Install frontend dependencies
    #   run: |
    #     cd ./frontend
    #     npm install

    # - name: Build application
    #   run: npm run build
    #   working-directory: ./frontend # Path to your client folder

    - name: Prepare SSH Directory
      run: |
        mkdir -p ~/.ssh
        echo "${{secets.SSH_PRIVATE_KEY}}" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh-keyscan ${{ secrets.HOST }} >> ~/.ssh/known_hosts
        # Debug key
        ssh-keygen -y -f ~/.ssh/id_rsa
        # Test SSH connection
        ssh -v -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ${{ secrets.USERNAME }}@${{ secrets.HOST }} "echo 'SSH connection successful'"

    - name: Deploy to VPS
      run: |
        ssh -v -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa ${{ secrets.USERNAME }}@${{ secrets.HOST }} 'bash -s' < ./scripts/deploy.sh
